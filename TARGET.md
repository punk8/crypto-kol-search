# Product Target

## 产品目标

把不同社交与内容平台中的 KOL、内容信号和品牌动作转化为可持续运行的增长闭环，同时保留每个平台的原生语义与安全边界。

```text
平台种子 → 持续发现 → KOL 晋升 → 信号扫描 → 平台内排序
        → 分级自动执行 / 人工审核 → 回执与确认回收
```

## 产品原则

- 平台账号彼此独立，不自动推断跨平台身份。
- 平台使用独立账号、内容、指标、评分和工作台。
- 核心只共享任务、机会、动作、策略、审计与控制状态。
- 新平台按能力渐进接入，可以先只读、后写入。
- 原始互动数不跨平台直接比较。
- 每个结果都必须追溯到平台原始对象与明确证据。
- 未确认写入永不自动重试。

## 当前范围

- X：发现、关系扩展、信号、趋势、评论、私信、自有内容发布和结果回收。
- 小红书：发现、Feed/趋势、评论和私信；不声明尚未实现的自有发布能力。
- 共享自动化：定时任务、KOL 晋升、策略分级、审批、执行、通道暂停和审计。

YouTube、Instagram 及其他平台当前只在架构上受到支持，不属于本轮真实集成范围。

## 验收标准

- 新平台模块能注册独立原生模型、能力、任务处理器和工作台，而无需修改核心状态机。
- X 与小红书同名账号不会合并。
- 每个平台独立产生 KOL 排名、内容机会和执行队列。
- 种子与 active KOL 能持续带来候选，平衡规则自动晋升合格账号。
- 高置信度评论和 X 自有发布可在双开关、额度与安全规则内自动执行。
- 首次冷私信始终需要审核；只有带证据的已验证原生会话才能作为后续私信，历史成功出站记录不自动建立会话。
- 单个平台失败不会停止其他平台或只读采集。
- 全局、平台、账号和动作类型 Kill Switch 均能阻止新写入。
- 任务、机会、策略决策、动作和回执完整可审计。
- 平台账号采用稳定轮转批次和每账号独立增量游标，单个大账号不会阻断其他账号扫描。
- 遗留运行任务和不确定写入不会自动重试；Postiz/浏览器回执必须唯一且可确认。
- AI 可选失败不阻断离线流程，也不能绕过任何确定性安全门或执行前复检。
- Mock 覆盖完整流程，完整 pytest 无真实凭据依赖。

## 暂不纳入

- 跨平台身份自动合并。
- 跨平台影响力总榜。
- 对外 SaaS、多租户、计费或分布式任务队列。
- 自动网页联系人采集。
- 写操作的自动重试或平台风控绕过。
